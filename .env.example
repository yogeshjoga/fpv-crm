# Supabase (client-side, safe to expose — protected by RLS)
VITE_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-or-publishable-key"

# Public base URL of this app, used to build certificate verification links
VITE_APP_URL="http://localhost:3000"

# --- Edge function secrets (set via `supabase secrets set`, NOT in this file for prod) ---
# RESEND_API_KEY="re_..."
# CERT_EMAIL_FROM="EgireRobotics <no-reply@mail.egirerobotics.com>"
